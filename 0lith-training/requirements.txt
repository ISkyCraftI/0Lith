# 0Lith Training — Dépendances Python
# GPU cible : RTX 5070 Ti 16 GB (Blackwell, CUDA 12.8)
# Python : 3.12
#
# Installation :
#   pip install -r requirements.txt
#
# Note Unsloth : installer depuis le repo Git (dernier commit) pour support Qwen3.5-4B + CUDA 12.8
# Note torch : PyTorch 2.6+ requis pour Blackwell (CC 12.0)

# Core training stack
unsloth[cu128-torch260] @ git+https://github.com/unslothai/unsloth.git
trl>=0.15.0
peft>=0.14.0
transformers>=4.51.0
accelerate>=1.4.0

# Datasets & data processing
datasets>=3.3.0
einops>=0.8.0
sentencepiece>=0.2.0

# Quantization (bf16 LoRA — bitsandbytes pour adamw_8bit optimizer uniquement, PAS pour QLoRA)
bitsandbytes>=0.46.0

# Experiment tracking
wandb>=0.19.0

# Utils
torch>=2.6.0
requests>=2.32.0
pyyaml>=6.0.0
